/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "xr1jblfnjocxsqzv.public.blob.vercel-storage.com",
      // add other blob hostnames here if needed
    ],
  },
};

export default nextConfig;
